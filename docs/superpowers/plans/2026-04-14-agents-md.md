# AGENTS.md Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Create a root-level `AGENTS.md` that helps both agents and human contributors understand the repo, work safely, and use the correct local/deploy commands.

**Architecture:** This change is documentation-only. The implementation should derive content from the existing repository structure, package scripts, and current React app organization, then encode that operational guidance in a standalone `AGENTS.md` at the repo root.

**Tech Stack:** Markdown, React 18, Create React App (`react-scripts`), styled-components, GitHub Pages

---

## File Structure

- Create: `AGENTS.md` — root-level repository operations guide for agents and human contributors
- Modify: `README.md` — optional cross-reference to `AGENTS.md` if desired during implementation review
- Reference: `package.json` — source of run/build/test/deploy commands and deployment config
- Reference: `src/App.js` — source of composition and theme architecture notes
- Reference: `src/index.js` — source of React entry-point notes
- Reference: `src/index.css` — source of global CSS / animation notes
- Reference: `docs/superpowers/specs/2026-04-14-agents-md-design.md` — approved design spec

### Task 1: Verify repository facts and draft the document skeleton

**Files:**
- Create: `AGENTS.md`
- Reference: `README.md`
- Reference: `package.json`
- Reference: `src/App.js`
- Reference: `src/index.js`
- Reference: `src/index.css`
- Reference: `docs/superpowers/specs/2026-04-14-agents-md-design.md`

- [ ] **Step 1: Write the failing test**

Create a manual documentation acceptance checklist in your working notes and treat any missing section as a failure:

```md
Required AGENTS.md sections:
- Project Overview
- Repository Map
- Local Development / Build / Test / Deploy commands
- Architecture Notes
- Editing Guidelines
- Warnings / Inconsistencies
- Verification Expectations
- Contributor / Agent Behavior
```

- [ ] **Step 2: Run test to verify it fails**

Run: review the repository and confirm that `AGENTS.md` does not yet exist.

```bash
find . -maxdepth 1 -name 'AGENTS.md'
```

Expected: no output

- [ ] **Step 3: Write minimal implementation**

Create `AGENTS.md` with the approved heading structure and short placeholder bullets derived from current repo facts.

```md
# AGENTS.md

## Project Overview
- React-based GitHub profile / portfolio site.
- Uses `react-scripts` and `styled-components`.
- Deploys to GitHub Pages.

## Repository Map
- `src/App.js`: main app composition and theme toggle.
- `src/components/`: section components.
- `src/index.js`: React entry point.
- `src/index.css`: baseline global CSS and animations.
- `public/`: static assets.
- `build/`: generated output.
- `README.md`: brief project summary.

## Local Development / Build / Test / Deploy
- `npm install`
- `npm start`
- `npm test`
- `npm run build`
- `npm run deploy`

## Architecture Notes
- Single-page section-based app.
- Theme state is managed in `src/App.js`.
- Styling is split across `styled-components` and `src/index.css`.

## Editing Guidelines
- Prefer small, localized edits.
- Follow existing React and `styled-components` patterns.
- Do not manually edit `build/`.

## Warnings / Inconsistencies
- Verify import paths and files under `src/components/`.
- Deployment depends on the `homepage` field in `package.json`.

## Verification Expectations
- Run `npm test` when behavior changes.
- Run `npm run build` before considering UI work complete.

## Contributor / Agent Behavior
- Follow existing structure first.
- Keep changes easy to review and revert.
```

- [ ] **Step 4: Run test to verify it passes**

Run: open `AGENTS.md` and verify all required headings from the checklist are present.

```bash
rg -n "^## " AGENTS.md
```

Expected output includes all eight required section headings.

- [ ] **Step 5: Commit**

```bash
git add AGENTS.md
git commit -m "docs: add AGENTS.md skeleton"
```

### Task 2: Expand each section into repo-specific operational guidance

**Files:**
- Modify: `AGENTS.md`
- Reference: `package.json`
- Reference: `src/App.js`
- Reference: `src/index.js`
- Reference: `src/index.css`

- [ ] **Step 1: Write the failing test**

Define the content assertions that the finished document must satisfy:

```md
AGENTS.md must explicitly mention:
- React 18 and Create React App / `react-scripts`
- `styled-components`
- GitHub Pages deployment
- `NODE_OPTIONS=--openssl-legacy-provider`
- `build/` should not be manually edited
- `src/App.js`, `src/index.js`, `src/index.css`, `src/components/`
- verification commands: `npm test`, `npm run build`
```

- [ ] **Step 2: Run test to verify it fails**

Run: search the current draft for the required exact operational details.

```bash
rg -n "React 18|react-scripts|styled-components|GitHub Pages|NODE_OPTIONS=--openssl-legacy-provider|build/|src/App.js|src/index.js|src/index.css|src/components/|npm test|npm run build" AGENTS.md
```

Expected: one or more required details are missing from the skeleton draft.

- [ ] **Step 3: Write minimal implementation**

Replace the short bullets with a complete, standalone guide like the following:

```md
# AGENTS.md

## Project Overview
This repository is a small React-based GitHub profile / portfolio site. It currently uses React 18 with Create React App conventions through `react-scripts`, styles UI with `styled-components`, and deploys the production build to GitHub Pages.

This file is intended for both coding agents and human contributors. Treat it as an operational guide for making safe changes, not as a product roadmap.

## Repository Map
- `src/App.js` — main composition root. It wires together the page sections, defines light/dark theme objects, injects themed global styles, and owns the theme toggle.
- `src/components/` — section-level UI components such as hero, header, about, projects, skills, and footer.
- `src/index.js` — React entry point that renders `<App />` inside `React.StrictMode`.
- `src/index.css` — baseline global CSS, scrollbar styling, and animation definitions.
- `public/` — static Create React App assets.
- `build/` — generated production output. Do not treat this as a source directory.
- `README.md` — brief public-facing summary of the project.
- `package.json` — scripts, dependencies, and GitHub Pages deployment configuration.

## Local Development / Build / Test / Deploy
Install dependencies:

```bash
npm install
```

Start the local development server:

```bash
npm start
```

Run the test suite:

```bash
npm test
```

Create a production build:

```bash
npm run build
```

Deploy to GitHub Pages:

```bash
npm run deploy
```

Notes:
- The existing `start`, `build`, and `predeploy` scripts set `NODE_OPTIONS=--openssl-legacy-provider`. Preserve that behavior unless you intentionally update the toolchain and verify the change.
- The GitHub Pages target path is controlled in part by the `homepage` field in `package.json`.

## Architecture Notes
The app is currently structured as a single-page, section-based React application. `src/App.js` imports and renders the major sections in order, so content and layout changes often stay localized to one component plus top-level composition.

Theme handling currently lives in `src/App.js`. That file defines the light and dark theme objects, wraps the tree in `ThemeProvider`, and injects shared themed styles through `createGlobalStyle`.

Global styling is split across two places: themed global styles inside `src/App.js` and non-theme baseline CSS inside `src/index.css`. When making visual changes, check both before assuming a style only exists in one place.

## Editing Guidelines
- Prefer small, localized edits over broad refactors.
- Follow the existing React function-component style and current `styled-components` usage.
- Keep theme-related values centralized when possible instead of duplicating color or spacing decisions across files.
- Avoid mixing unrelated cleanup with a targeted content or UI change.
- Do not manually edit files in `build/`; regenerate them through the build process.
- Before moving or renaming components, verify imports from `src/App.js` and any files under `src/components/`.

## Warnings / Inconsistencies
- Styling is intentionally split between `styled-components` and `src/index.css`. Visual bugs may come from either layer.
- Deployment is coupled to GitHub Pages configuration in `package.json`, especially the `homepage` value and deploy script.
- This is a small repository, so path mismatches or renamed files under `src/components/` can break the app quickly. Re-check imports after structural edits.
- `README.md` is concise and useful for project summary, but it does not capture all operational details contributors may need.

## Verification Expectations
After changing code or documentation that affects behavior, use the smallest meaningful verification step and escalate as needed:
- For component logic or behavior changes, run `npm test`.
- For UI, styling, structure, or deployment-related changes, run `npm run build` before calling the work complete.
- When changing deploy behavior, confirm the `homepage` and deploy target still match the intended GitHub Pages URL.

## Contributor / Agent Behavior
- Follow existing repository patterns before introducing new abstractions.
- Keep changes easy to review, explain, and revert.
- State assumptions clearly when making non-obvious decisions.
- Avoid speculative refactors unless they directly support the requested task.
- If you update project structure or workflow, update this file so future contributors inherit accurate guidance.
```

- [ ] **Step 4: Run test to verify it passes**

Run these checks:

```bash
rg -n "React 18|react-scripts|styled-components|GitHub Pages|NODE_OPTIONS=--openssl-legacy-provider|Do not manually edit files in `build/`|src/App.js|src/index.js|src/index.css|src/components/|npm test|npm run build" AGENTS.md
```

Expected: every required detail is matched in the document.

- [ ] **Step 5: Commit**

```bash
git add AGENTS.md
git commit -m "docs: expand AGENTS.md guidance"
```

### Task 3: Verify the final document against the repository and optionally cross-link from README

**Files:**
- Modify: `AGENTS.md`
- Modify: `README.md` (only if adding a short pointer is helpful and desired)
- Reference: `package.json`
- Reference: `docs/superpowers/specs/2026-04-14-agents-md-design.md`

- [ ] **Step 1: Write the failing test**

Create the final acceptance checklist:

```md
Final acceptance criteria:
- `AGENTS.md` exists at repo root
- Content matches current package scripts and architecture
- It discourages manual edits to `build/`
- It includes commands and verification expectations
- It is readable as a standalone guide for both agents and humans
```

- [ ] **Step 2: Run test to verify it fails**

Run manual comparison between `AGENTS.md` and repo facts. If any script name, path, or architecture note is inaccurate, the task fails.

Commands:

```bash
readlink -f AGENTS.md
node -e "const p=require('./package.json'); console.log(JSON.stringify({scripts:p.scripts, homepage:p.homepage}, null, 2))"
```

Expected before final review: either no `AGENTS.md` path if earlier tasks were skipped, or discrepancies found during comparison that still need correction.

- [ ] **Step 3: Write minimal implementation**

Make any final wording corrections needed so `AGENTS.md` exactly matches current repository reality. Optionally add a short line to `README.md` pointing contributors to `AGENTS.md` for repo workflow guidance.

If updating `README.md`, append a brief note such as:

```md
## Contributor Notes

For repository workflow, commands, and editing guidance, see `AGENTS.md`.
```

- [ ] **Step 4: Run test to verify it passes**

Run:

```bash
npm run build
```

Expected: successful production build with no blocking errors.

Then run:

```bash
rg -n "^# AGENTS.md|^## Project Overview|^## Repository Map|^## Local Development / Build / Test / Deploy|^## Architecture Notes|^## Editing Guidelines|^## Warnings / Inconsistencies|^## Verification Expectations|^## Contributor / Agent Behavior" AGENTS.md
```

Expected: all required sections are present.

- [ ] **Step 5: Commit**

```bash
git add AGENTS.md README.md
git commit -m "docs: finalize contributor guide"
```

## Self-Review

- **Spec coverage:** The tasks cover overview, file map, commands, architecture, editing guidance, warnings, verification, and audience fit for both humans and agents.
- **Placeholder scan:** No TODO/TBD markers are left in executable steps; each code-writing step includes actual content.
- **Type consistency:** File paths, commands, and section names are used consistently across all tasks.
